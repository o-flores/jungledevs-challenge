import React from 'react';
import './style.css';
import Header from '../Header';

function Hero() {
  return(
    <section className="hero">
      <Header />
    </section>
  );
};

export default Hero;
